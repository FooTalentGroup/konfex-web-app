[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npx tsc && npx tsc-alias"]

[start]
cmd = "npx prisma generate && npx prisma migrate deploy && node dist/src/index.js"
